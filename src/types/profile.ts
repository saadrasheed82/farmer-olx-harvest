export interface Profile {
  id: string;
  email: string;
  full_name: string;
  avatar_url?: string;
  phone?: string;
  whatsapp?: string;
  location_city?: string;
  location_province?: string;
  created_at: string;
  updated_at: string;
} 